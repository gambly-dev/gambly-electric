apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gambly-electric
  namespace: ${NAMESPACE}
  labels:
    app: gambly-electric
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  storageClassName: ""
  volumeName: ${PV_NAME}
