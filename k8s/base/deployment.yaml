apiVersion: apps/v1
kind: Deployment
metadata:
  name: gambly-electric
  namespace: ${NAMESPACE}
  labels:
    app: gambly-electric
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gambly-electric
  template:
    metadata:
      labels:
        app: gambly-electric
    spec:
      containers:
        - name: gambly-electric
          image: electricsql/electric:1.4.4
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: gambly-electric-env
          env:
            - name: ELECTRIC_STORAGE_DIR
              value: /var/lib/electric/persistent
            - name: ELECTRIC_SHAPE_DB_STORAGE_DIR
              value: /var/lib/electric/shape-db
            - name: ELECTRIC_SERVICE_NAME
              value: gambly-electric
            - name: ELECTRIC_SHAPE_DB_EXCLUSIVE_MODE
              value: "true"
            - name: ELECTRIC_EXPERIMENTAL_MAX_SHAPES
              value: "10000"
            - name: ELECTRIC_SHAPE_DB_SYNCHRONOUS
              value: NORMAL
          volumeMounts:
            - name: electric-storage
              mountPath: /var/lib/electric/persistent
              subPath: persistent
            - name: electric-storage
              mountPath: /var/lib/electric/shape-db
              subPath: shape-db
          livenessProbe:
            httpGet:
              path: /v1/health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 2
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - test "$(curl -so /dev/null -w '%{http_code}' http://localhost:3000/v1/health)" = "200"
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            requests:
              cpu: "1"
              memory: 4Gi
            limits:
              cpu: "1"
              memory: 4Gi
      volumes:
        - name: electric-storage
          persistentVolumeClaim:
            claimName: gambly-electric
